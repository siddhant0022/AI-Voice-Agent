import React from 'react'

function Provider({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  const CheckNewUser = () => {
    
  }
  return (
    <div> {children} </div>
  )
}

export default Provider